utlp.Menu = function(game){
};

utlp.Menu.prototype = {
    create: function(){ 
      this.add.text(10, 10, 'menu', {fill: '#fff'});
       // this.scale.forceOrientation(false, true);
        
    },
    update: function() {
        
    }
};
